apiVersion: v1
kind: ConfigMap
metadata:
  name: env-variable
data:
  # property-like keys; each key maps to a simple value
  POSTGRES_HOST: udagram.cq4g1jgpebog.us-east-1.rds.amazonaws.com
  POSTGRES_DB: postgres
  AWS_BUCKET: yso-udagram
  AWS_REGION: us-east-1
  AWS_PROFILE: default
  URL : http://localhost:8100